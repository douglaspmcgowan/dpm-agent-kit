---
name: hue
description: Meta-skill that generates new design language skills. Use when the user says 'create a design skill', 'generate design language', 'new design system skill', 'design skill inspired by X', 'design skill from this screenshot', '/hue', or 'use hue'. Also triggers for 'remix my design skill' or 'make my skill more X'.
---

# /hue

Full skill lives at `~/.claude/skills/hue/SKILL.md` (831 lines + `references/` templates + `examples/` design skills). Read that file and follow its instructions — it uses relative paths to `references/` and `examples/` that resolve correctly from the skill directory.

Upstream: `https://github.com/dominikmartn/hue` — `cd ~/.claude/skills/hue && git pull` to update.
